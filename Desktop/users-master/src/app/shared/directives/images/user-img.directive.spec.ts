import { UserImgDirective } from './user-img.directive';

describe('UserImgDirective', () => {
  it('should create an instance', () => {
    const directive = new UserImgDirective();
    expect(directive).toBeTruthy();
  });
});
